<form data-submit="ajax"
      class="form-horizontal"
      action="{{ path('_demo_contact') }}"
      method="POST">

    {{ form_rest(form) }}

    <button type="submit"
            class="btn btn-primary pull-right">
        Send it
    </button>
</form>